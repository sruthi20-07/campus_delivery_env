[project]
name = "campus-delivery-env"
version = "0.1.0"
description = "Campus delivery fraud detection OpenEnv environment"
authors = [{ name = "Sruthi Vindula" }]

dependencies = [
    "pydantic",
    "fastapi",
    "uvicorn",
    "openai",
    "openenv"
]

[project.scripts]
server = "server.app:app"

[tool.openenv]
env_class = "env.environment:DeliveryEnv"