[project]
name = "campus-delivery-env"
version = "0.1.0"
description = "Campus delivery fraud detection OpenEnv environment"
authors = [
    { name = "Sruthi Vindula" }
]
dependencies = [
    "pydantic",
    "fastapi",
    "uvicorn",
    "openai"
]

[tool.openenv]
entry_point = "env.environment:DeliveryEnv"